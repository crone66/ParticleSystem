namespace ParticleSystem
{
    public struct InstancingAlpha
    {
        public float alpha;

        public InstancingAlpha(float alpha)
        {
            this.alpha = alpha;
        }
    }
}
